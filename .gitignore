# OS
.DS_Store
Thumbs.db

# Editors / agents
.vscode/
.idea/
.claude/*
# ...except the shared project settings, which carry the audit Stop hook
!.claude/settings.json

# Dev backups — not part of the deployable site
*.backup-*.html

# Node (only if Shopify headless tooling is added later)
node_modules/
npm-debug.log*
